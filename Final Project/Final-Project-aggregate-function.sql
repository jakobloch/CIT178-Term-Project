/*USE Hotel;
SELECT MIN(StartDate) AS 'Earliest Reservation Date'
FROM Reservation;

USE Hotel;
SELECT MAX(EndDate) AS 'Latest Check Out Date'
FROM Reservation;

USE Hotel;
SELECT COUNT(GuestID) AS 'Number of Guests'
FROM Guest;

USE Hotel;
Select AVG(Cost) AS 'Average Cost Per Room'
FROM Room;*/